

//enum for changing the selectedOption of the select options in the todoInput to filter out the todos
export enum selectedOption{
    ALL = "all",
    COMPLETED = "completed",
    INCOMPLETE = "incomplete"
}